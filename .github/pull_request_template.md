## Task and authority

- Task contract:
- Candidate SHA:
- Accepted base SHA:
- Change class:
- Allowed paths:
- Protected or normative paths touched:

## Change

Describe one bounded logical change.

## Evidence and provenance

List exact sources, versions, spans, repository commits, datasets, methods, prompts/models where relevant, and lineage limitations.

## Validation

- [ ] Accepted repository validation passed
- [ ] Deterministic public build passed
- [ ] Unit and regression tests passed
- [ ] Relevant adversarial tests passed
- [ ] Disclosure noninterference audit passed
- [ ] Append-only audit passed where applicable
- [ ] Generated state is current and not hand-edited
- [ ] Run receipt appended

Commands and outputs:

```text

```

## Security and disclosure

State how untrusted content, secrets, private data, permissions, external calls, and public projections are affected.

## Limitations and negative results

Record unresolved questions, failures, excluded scope, and dependencies. Do not omit them to make the contribution appear complete.

## Integration

The contributor does not authorize its own normative change. A trusted integrator must load authority from the accepted base, pin this candidate SHA, and record an immutable decision.
