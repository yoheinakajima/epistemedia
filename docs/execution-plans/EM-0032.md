# EM-0032 execution plan

Status: author research packet complete; fresh-clone independent review pending.

## Objective

Turn the accepted GPT-4 bar-exam readiness map into a content-addressed candidate research packet
that separates the historical composite score from every percentile comparison class, reconstructs
disclosed calculations, and leaves the missing launch derivation unresolved.

## Authority and identity

- immutable task: `tasks/contracts/EM-0032.json`;
- accepted base: `7dca84b0c8477efc09b31215f734348d626957d4`;
- branch: `codex/em-0032-gpt4-bar-lineage`;
- accepted preliminary packet:
  `research/how-we-know/library-preliminary/gpt-4-bar-exam-percentile.md`;
- allowed paths: the EM-0032 research directory, this plan, the Case 003 research note, and
  append-only run receipts.

## Method

1. Freeze the narrow historical target and a `2026-08-27` evidence cutoff.
2. Capture credential-free public primary/authoritative carriers with exact URL, edition, bytes,
   SHA-256, license treatment, and quote-minimal spans.
3. Verify the 15 preliminary core sources and 89-file mechanical artifact inventory.
4. Collapse report, preprint, VOR, repository, supplement, and OSF manifestations by underlying
   experiment or analysis root.
5. Separate observed/reported score, pass threshold, first-time/repeater status, passers,
   administration, and modeled percentile.
6. Recompute the disclosed Illinois sensitivities and Martínez assumptions deterministically.
7. Preserve `298/~297`, Martínez `45/48`, the OSF `0.27/24%` comment conflict, and the
   `266/270` cutoff difference rather than silently choosing one value.
8. Keep the launch chart, interpolation, national total-UBE distribution, purchased inputs,
   proprietary snapshots, and official essay grading visible as unresolved limitations.
9. Require a fresh-clone reviewer to re-retrieve sources, match every span, reproduce the math and
   lineage, and bind a GO/HOLD/FAIL receipt to the exact author head.

## Deterministic packet

- packet ID:
  `em:research-packet:sha256:3302c2c7282699e52ab53d670a83ded21f2a43de7685604075a1eed8a2f63ae1`;
- artifact inventory ID:
  `em:artifact-inventory:sha256:715844dcdcc9464e0f2b86e7d47f18145865bffb9fc7d02351836b53488621b9`;
- 19 source records, including 15 preliminary core objects;
- 32 quote-minimal spans;
- 89 file metadata records with zero independent-evidence credit;
- 8 scoped claim records, 10 derivations, 5 lineages, and 1 bounded negative search.

The author recommendation is **GO pending independent review** for later dossier construction. It
does not promote the packet or establish a public verdict.

## Validation gates

- deterministic packet rebuild;
- exact capture identity and artifact-root counts;
- independent analytic recomputation of every displayed derivation;
- sentence-to-span and source-to-lineage closure;
- explicit no-credit handling for the unresolved launch derivation and artifact mirrors;
- fail-closed independent-review gate;
- Ruff, JSON parsing, path-scope comparison, `git diff --check`, full `make check`, disclosure
  audit, deterministic public rebuild, and clean tracked state.

## Author validation record

- `build_packet.py --check` reproduced the committed packet and its content address;
- `verify_packet.py` independently recomputed all ten calculations and verified 19 source
  captures, 15 core objects, 32 spans, and all 89 artifact records;
- `verify_packet.py --require-review` failed closed with
  `independent review receipt missing`, as required before review;
- Ruff passed for both research Python files;
- `make check` passed accepted-input validation, the complete test suite, disclosure audit with no
  findings, deterministic independent rebuild, and unchanged accepted source state; and
- append-only author receipt: `runs/proposals/20260827T155153Z-29d70e6b9e44.json`.

An independent exact-head receipt and protected merge evidence remain pending.

## Boundaries

No dossier construction, catalog admission, public verdict, Pages deployment, current-model
ranking, provider call, proprietary rerun, purchased input, credential, spend, DNS, workflow,
package, or release action is authorized.
