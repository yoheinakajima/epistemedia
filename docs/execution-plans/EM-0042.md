# EM-0042 — Close the second cold-start selection and clock gaps

## Objective

Preserve autonomous submission PR #72 as the immutable second cold-start receipt, fix the exact-head
CI binding it exposed, prevent agents from selecting accepted or closely restated work, and replace
contributor-invented runtime timestamps with executable-captured start and completion times before a
third cold start.

## Accepted evidence and boundaries

- Accepted base: `71439e9bb3e95a0121956a9721e736bd91961034`.
- Frozen queue receipt: PR #72 at head `3f965831031b88f4efff954550ecb383fec0e5a8`, tree
  `c67de1a45bd9d97aac6cd39c0f63aa73a30b3a2d`.
- The packet remains an untrusted, blocking, open draft and is never repaired or merged.
- Cases 001–004, PR #69, accepted open dockets, and existing public meanings remain unchanged.
- No admission, promotion, numbered case, merge, deployment, or further provider run occurs until
  the implementation passes exact-head independent review and protected integration.

## Implementation

1. Check out the immutable pull-request source head in CI rather than GitHub's synthetic merge ref.
2. Require the accepted base commit time to precede the declared runtime chronology.
3. Have `research prepare` record `runtime.started_at`, `research complete` record
   `runtime.completed_at` before fail-closed validation, and `research submit` re-seal completion
   immediately before canonicalizing queue bytes.
4. Reject lexical near-duplicates of accepted dossier and reviewed open-docket questions, while
   instructing agents to inspect open queue PRs and record their prior-art comparison.
5. Add focused regressions, run the full deterministic repository check, and obtain a fresh
   non-author exact-head review before any protected merge.

## Status

- 2026-08-30: implementation started from accepted main after the live PR #72 failure receipt was
  captured. No contributor bytes, accepted dossier, deployment, or public queue state were changed.
- 2026-08-30: the focused workflow, research-kit, and open-docket suite passed; changed Python
  surfaces passed defect-focused Ruff; workflow YAML parsed; and full `make check` passed accepted
  input validation, the complete test suite, disclosure audit, deterministic rebuild, and source-tree
  state verification. The resulting local catalog contains 219 repository objects across 11 topics.
