# EM-0025 execution plan

Status: candidate complete on `codex/em-0025-concept-and-case-roadmap`; exact-head review pending.

## Objective

Preserve the owner-directed conceptual material behind Epistemedia's name, synthesize the supplied
Case 002 strategy without laundering model-generated pre-research into evidence, select a bounded
Case 002 direction, and retain a diverse small-library roadmap.

## Inputs and authority

- accepted base: `2f9265ffb389e06ca7ef102c73678936d8b0136d`;
- immutable task: `tasks/contracts/EM-0025.json`;
- owner direction and three supplied strategy/pre-research packets from 2026-08-22;
- accepted `docs/product-direction.md`, `docs/brand.md`, `docs/dossiers.md`, and root `AGENTS.md`.

The supplied packets are private coordination inputs. Their factual leads may become retrieval
questions, but their prose, reported numbers, and implied citations are not repository evidence.

## Change surface

1. Add a conceptual-vocabulary document under `docs/editorial/`.
2. Add a standalone Case 002 decision, candidate ledger, research brief, and library roadmap.
3. Link both from the documentation index and product-direction brief.
4. Record claim and completion receipts under `runs/`.

## Boundaries

- no Case 002 source collection, agent run, empirical assertion, dossier, catalog entry, or feature;
- no change to Case 001, application code, tests, policies, schemas, workflows, or generated files;
- no raw private model conversation or unverified numeric finding copied into public documentation;
- no deployment, promotion, spend, credentials, accounts, or provider action.

## Verification

- compare the case ledger against every supplied candidate and strategic criterion;
- verify the vocabulary includes the Greek distinctions and their product consequences while
  preserving the philosophy-versus-product boundary;
- search for accidental claims of accepted evidence, Case 002 completion, or universal knowledge;
- verify exact changed-path scope against EM-0025;
- run `make check PYTHON=.venv/bin/python` and `git diff --check`;
- obtain exact-head independent review before protected merge.

## Candidate results

- both new editorial documents are linked from the documentation index and product brief;
- the vocabulary retains the Greek cluster, historical-sense boundary, modern operational bridge,
  product implications, explicit non-authority statement, and future source-packet agenda;
- the case record includes all supplied candidate classes, separates selection from admission,
  selects agent citation lineage conditionally, defines a trace-first unit and provisional card,
  preserves later retrieval questions, and sequences Cases 002–006;
- changed paths are limited to `docs/**` and the claim/completion receipts under `runs/**`;
- `make check PYTHON=.venv/bin/python` passes 80 tests, accepted-input validation, a clean public
  audit, deterministic comparison build, and unchanged source-tree state;
- `git diff --check` passes.
