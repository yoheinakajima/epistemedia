# EM-0046 — Live-service documentation reconciliation

## Goal

Make every current public project description tell the same post-activation truth: the anonymous,
read-only REST and Streamable HTTP MCP gateway is live from the accepted v0.2.0 image, while
authenticated submission and every unrelated publication channel remain unavailable.

## Sequence

1. Re-read current Git, tag, GHCR, DNS, TLS, REST, and MCP identities from public sources.
2. Add one append-only activation receipt rather than modifying historical records that accurately
   describe what was not live when they were written.
3. Update the README, mission current-state flags, current interface/launch/hosting documentation,
   status-page compiler copy, `llms.txt` compiler copy, and their regression tests.
4. Preserve a sharp distinction between live anonymous reads and the unavailable authenticated MCP
   submission queue.
5. Run focused and full deterministic checks, append a bounded completion receipt, obtain exact-head
   independent review, and stop before merge or any coordinated release/deployment action.

## Stop conditions

- Do not edit accepted cases, open-docket research, evidence, verdicts, policies, schemas,
  constitutional rules, or historical activation receipts.
- Do not call a free-compute preview an always-on production SLA.
- Do not publish a GitHub Release, PyPI package, MCP Registry entry, new container, or new tag.
- Do not merge, deploy Pages, redeploy Render, change DNS, grant credentials, or add write authority.

## Status

- 2026-09-02: owner requested full documentation reconciliation after the v0.2.0 image and gateway
  activation. Work began from a clean clone of exact main
  `f92846570180dfa4511263f8ba98ecd18f7772c9`.
- 2026-09-02: public DNS returned both custom-domain CNAMEs to
  `epistemedia-gateway.onrender.com`. Direct TLS probes for the two Render edge addresses succeeded.
  REST `/v1/status` and MCP `server/discover` returned the same commit, catalog, frontier, policy
  pack, and compiler as the accepted v0.2.0 static release.
- Historical activation and completion records are being preserved byte-for-byte. This change will
  add a superseding receipt and update only current-state sources and projections.
- 2026-09-02: focused mission/interface tests passed. The full repository check passed all tests,
  returned zero disclosure findings, reproduced the public projection independently, and confirmed
  that the source tree was unchanged by validation. The candidate remains unmerged and undeployed;
  exact-head CI, independent review, integration, and coordinated static/runtime release parity are
  later gates.
